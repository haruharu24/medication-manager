import type { CapacitorConfig } from '@capacitor/cli';

// appId is a placeholder — replace with the real bundle identifier registered in
// App Store Connect before running `npx cap add ios` (see README's iOS section).
const config: CapacitorConfig = {
  appId: 'com.example.medimate',
  appName: 'MediMate',
  webDir: 'dist',
};

export default config;
